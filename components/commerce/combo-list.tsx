"use client";
import Image from "next/image"; import { formatCRC } from "@/lib/format"; import { useStore } from "./store-provider"; import type { Locale } from "@/lib/i18n";
type Combo={id:string;slug:string;name:string;description:string|null;imageUrl:string|null;price:number;promotionalPrice:number|null;items:{name:string;quantity:number}[]};
export function ComboList({combos,locale}:{combos:Combo[];locale:Locale}){const{addItem}=useStore();const es=locale==="es";if(!combos.length)return <div className="search-results"><section><h2>{es?"Próximamente nuevos combos":"New gift sets coming soon"}</h2><p>{es?"Mientras tanto puedes crear tu regalo paso a paso.":"In the meantime, you can build your gift step by step."}</p></section></div>;return <div className="combo-grid">{combos.map(combo=>{const price=combo.promotionalPrice??combo.price;return <article className="combo-card" key={combo.id}>{combo.imageUrl&&<div className="combo-image"><Image src={combo.imageUrl} alt={combo.name} fill sizes="(max-width:700px) 100vw, 33vw"/></div>}<div><p className="commerce-kicker">{es?"Combo":"Gift set"}</p><h2>{combo.name}</h2><p>{combo.description}</p><ul>{combo.items.map(item=><li key={`${item.name}-${item.quantity}`}>{item.quantity} × {item.name}</li>)}</ul><p className="combo-price">{combo.promotionalPrice&&<del>{formatCRC(combo.price)}</del>} <strong>{formatCRC(price)}</strong></p><button className="commerce-primary" type="button" onClick={()=>addItem({productId:combo.id,slug:`combo-${combo.slug}`,name:combo.name,price,quantity:1,image:combo.imageUrl,personalization:`${es?"Combo":"Gift set"}: ${combo.items.map(item=>item.name).join(", ")}`})}>{es?"Agregar combo":"Add gift set"}</button></div></article>})}</div>}
