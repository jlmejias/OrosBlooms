import Image from "next/image";
import { AdminRecordActions } from "./admin-record-actions";
import { deleteProductImage, deleteProductVariant, saveProductImage, saveProductVariant } from "@/app/admin/actions";

type Variant={id:string;name:string;price:number;available:boolean;sortOrder:number};
type ProductImage={id:string;url:string;alt:string|null;primary:boolean;sortOrder:number};
export function ProductAssetsEditor({productId,variants,images}:{productId:string;variants:Variant[];images:ProductImage[]}){return <div className="admin-product-details"><section className="admin-panel"><h2>Variantes</h2><div className="admin-split-content"><form action={saveProductVariant} className="admin-form"><input type="hidden" name="productId" value={productId}/><label>Nombre<input name="name" placeholder="Ej. Grande" required/></label><label>Precio<input name="price" type="number" placeholder="0" min="0" required/></label><label>Orden<input name="sortOrder" type="number" placeholder="0" min="0"/></label><label><input name="available" type="checkbox" defaultChecked/> Disponible</label><button>Agregar variante</button></form><table className="admin-table"><thead><tr><th>Variante</th><th>Precio</th><th>Estado</th><th>Acciones</th></tr></thead><tbody>{variants.map(item=><tr key={item.id}><td>{item.name}</td><td>₡{item.price.toLocaleString("es-CR")}</td><td>{item.available?"Disponible":"Oculta"}</td><td><AdminRecordActions id={item.id} deleteAction={deleteProductVariant}/></td></tr>)}</tbody></table></div></section><section className="admin-panel"><h2>Imágenes</h2><div className="admin-split-content"><form action={saveProductImage} className="admin-form"><input type="hidden" name="productId" value={productId}/><label>URL de imagen<input name="url" type="url" placeholder="https://..." required/></label><label>Descripción<input name="alt" placeholder="Describe la imagen" required/></label><label>Orden<input name="sortOrder" type="number" placeholder="0" min="0"/></label><label><input name="primary" type="checkbox"/> Imagen principal</label><button>Agregar imagen</button></form><div className="admin-media-grid">{images.map(item=><article key={item.id}><div><Image src={item.url} alt={item.alt??""} fill unoptimized/></div><span>{item.primary?"Principal":`Orden ${item.sortOrder}`}</span><AdminRecordActions id={item.id} deleteAction={deleteProductImage}/></article>)}</div></div></section></div>}
