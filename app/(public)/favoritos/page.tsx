import { PublicShell } from "@/components/commerce/public-shell"; import { SavedProducts } from "@/components/commerce/saved-products"; import { listProducts } from "@/services/catalog"; import { getLocale } from "@/lib/i18n";
export default async function FavoritesPage(){const [products,locale]=await Promise.all([listProducts(),getLocale()]);const es=locale==="es";return <PublicShell><div className="commerce-wrap"><header className="commerce-hero"><p className="commerce-kicker">{es?"Tu selección":"Your selection"}</p><h1>{es?"Flores favoritas.":"Favorite flowers."}</h1><p>{es?"Esta lista se guarda únicamente en este dispositivo.":"This list is saved only on this device."}</p></header><SavedProducts products={products} locale={locale}/></div></PublicShell>}
