import { PublicShell } from "@/components/commerce/public-shell"; import { CartView } from "@/components/commerce/cart-view"; import { getLocale } from "@/lib/i18n";
export default async function CartPage(){const locale=await getLocale();const es=locale==="es";return <PublicShell><div className="commerce-wrap"><header className="commerce-hero"><p className="commerce-kicker">{es?"Tu selección":"Your selection"}</p><h1>{es?"Carrito.":"Cart."}</h1><p>{es?"Revisa cantidades y dedicatorias antes de enviar tu solicitud.":"Review quantities and messages before sending your request."}</p></header><CartView locale={locale}/></div></PublicShell>}
