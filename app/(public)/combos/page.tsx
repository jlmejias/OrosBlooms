import type { Metadata } from "next"; import { ComboList } from "@/components/commerce/combo-list"; import { PublicShell } from "@/components/commerce/public-shell"; import { listCombos } from "@/services/catalog"; import { getLocale } from "@/lib/i18n";
export const metadata:Metadata={title:"Gift sets",description:"Seasonal OrosBlooms floral gift sets.",alternates:{canonical:"/combos"}};
export default async function CombosPage(){const [combos,locale]=await Promise.all([listCombos(),getLocale()]);const es=locale==="es";return <PublicShell><div className="commerce-wrap"><header className="commerce-hero"><p className="commerce-kicker">{es?"Combos":"Gift sets"}</p><h1>{es?"Regalos listos para emocionar.":"Gifts made to delight."}</h1><p>{es?"Combinaciones cuidadas con vigencia y precio propios.":"Thoughtful combinations with their own availability and price."}</p></header><ComboList combos={combos} locale={locale}/></div></PublicShell>}
