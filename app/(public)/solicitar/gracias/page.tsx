import Link from "next/link"; import { PublicShell } from "@/components/commerce/public-shell"; import { stringValue } from "@/lib/format";
export default async function ThanksPage({searchParams}:{searchParams:Promise<Record<string,string|string[]|undefined>>}){const p=await searchParams;return <PublicShell><div className="commerce-wrap"><header className="commerce-hero"><p className="commerce-kicker">Solicitud recibida</p><h1>Tu historia ya empezó a florecer.</h1><p>Guarda esta referencia: <strong>{stringValue(p.ref)}</strong>. Nos pondremos en contacto para continuar.</p><p><Link href="/flores">Volver al catálogo</Link></p></header></div></PublicShell>}
