import { count, eq, sql } from "drizzle-orm"; import Link from "next/link"; import { AdminDataTable } from "@/components/admin/admin-data-table"; import { db } from "@/db/client"; import { customers,inquiries,orders,products } from "@/db/schema"; import { requireAdmin } from "@/lib/admin-auth";
export default async function AdminDashboard(){await requireAdmin();const[[productCount],[inquiryCount],[orderCount],[customerCount],recent]=await Promise.all([db.select({value:count()}).from(products).where(eq(products.status,"active")),db.select({value:count()}).from(inquiries),db.select({value:count()}).from(orders),db.select({value:count()}).from(customers),db.select({reference:inquiries.reference,status:inquiries.status,createdAt:inquiries.createdAt}).from(inquiries).orderBy(sql`${inquiries.createdAt} desc`).limit(6)]);const statusOptions=[...new Set(recent.map(item=>item.status))].map(value=>({value,label:value}));return <><header className="admin-head"><div><h1>Resumen</h1><p>Actividad de OrosBlooms.</p></div><Link className="admin-button" href="/admin/solicitudes">Ver solicitudes</Link></header><section className="admin-stats"><article className="admin-stat"><span>Productos activos</span><strong>{productCount.value}</strong></article><article className="admin-stat"><span>Solicitudes</span><strong>{inquiryCount.value}</strong></article><article className="admin-stat"><span>Pedidos</span><strong>{orderCount.value}</strong></article><article className="admin-stat"><span>Clientes</span><strong>{customerCount.value}</strong></article></section><div className="admin-dashboard-list"><AdminDataTable columns={["Referencia","Estado","Fecha","Acciones"]} rows={recent.map(item=>({id:item.reference,cells:[{primary:item.reference},{primary:item.status,tone:item.status==="completed"||item.status==="approved"?"active" as const:item.status==="rejected"?"inactive" as const:"warning" as const},item.createdAt.toLocaleDateString("es-CR")],filterValue:item.status}))} noun="solicitudes" searchPlaceholder="Buscar solicitudes recientes..." filterPlaceholder="Todos los estados" filterOptions={statusOptions}/></div></>}
